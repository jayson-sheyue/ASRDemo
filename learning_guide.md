# 听写实验室 · 入门指南

这份指南假设你**第一次**听到「语音识别」。不需要懂编程、机器学习、Google Cloud，也不需要知道 V1、V2、Recognize 这些词——下面会用大白话解释。

**读完并点几下 Demo 之后，你应该能回答：**

1. 这个产品是干什么的
2. 「同步 / 实时 / 批量」三种送声音的方式差在哪、各自值多少钱精力
3. 顶栏五页分别给谁用
4. 医学模型为什么只有英语
5. 出问题该打开哪份官方文档

请先按 [README.md](README.md) 把页面打开。没有登录也可以读文档、点样例、用「预览请求」；**只有点「转写」或「实时听写」才会把声音发到 Google，并可能产生费用。**

资料核查日期：2026-09-10。本项目不是 Google 官方产品。

---

## 0. 它到底是什么（先建立图像）

### 一句话

**Speech-to-Text = 语音识别 = 把已经说出来的声音，变成可以阅读、可以搜索、可以存档的文字。**

英文缩写你会看到两个，意思一样：

- **STT**：Speech-to-Text（语音变成文字）
- **ASR**：Automatic Speech Recognition（自动语音识别）

它**不是**：

- 聊天机器人（不会回答「今天天气怎么样」）
- 同声传译员（Chirp 2 能做「听英语写出中文」，但那是附加能力，不是默认）
- 录音笔本身（它不负责把声音录下来长期保存）
- 能认人的门禁（听不出「这是张三本人」）

你可以把它想成：**一位只负责打字的速记员。** 你把录音交给它，它交回一份稿。稿可能有错别字、专有名词写不对，也不能当法庭上的逐字记录。

### 工作台在干什么

| 你在页面上点的 | 生活里像什么 | 业务上换来什么 |
| --- | --- | --- |
| 选一段样例 / 上传录音 / 对着麦说 | 把录音笔或电话录音交给速记员 | 不用人从头听写 |
| 选 Chirp 3 / 电话 / 医学 | 换一位「听过很多会议室」或「听过很多电话」或「听过很多病历」的速记员 | 同类音频更准，账单模型也可能不同 |
| 选语言 `cmn-Hans-CN` | 告诉对方「这是普通话」 | 用错语言，准确率会明显变差 |
| 填写专有名词 | 会前塞一张人名、产品名清单 | 项目 ID、药名、品牌不那么容易写错 |
| 打开说话人分离 | 请在稿子上标注「这句是谁说的」 | 会议纪要能按人分段 |
| 点「转写」 | 「请把整段录音听完再交稿」 | 适合已有文件、短于约 1 分钟 |
| 点「实时听写」 | 「请边听边打字幕」 | 适合直播字幕、坐席辅助、现场记录 |

---

## 1. 三个必须先懂的词：同步、实时、批量

Google 把「怎么把声音送过去」分成三条路。名字看起来像编程，其实是三种交货方式。

### 1.1 同步 Recognize（本 Demo 点「转写」走这条）

**人话：** 把整段音频一次性交给云端，等它全部听完，一次性把全文给你。

**限制：** 官方建议音频大约短于 **1 分钟**。再长，这条路会拒绝或很不稳定。

**业务价值：**

- 客服电话留言、语音消息、短口播、几秒到几十秒的指令
- 实现最简单：上传 → 等待 → 得到一篇稿
- 用户体验是「转圈，然后全文出现」，不是逐字往外蹦

**不适合：** 一小时会议、整天呼叫中心录音。

官方接口名：`Recognize`（V2）或 V1 里同类的一次性识别。

### 1.2 实时 StreamingRecognize（本 Demo 点「实时听写」）

**人话：** 麦克风开着，话说到一半，屏幕上就开始出字；说完一句，那一句会「定稿」。

**和「先录音再转写」不是一回事。** 先录 20 秒再点转写，仍然是同步 Recognize，只是录音发生在浏览器里。实时听写才是边说边传。

**业务价值：**

- 直播/会议字幕（晚半拍也能接受）
- 客服坐席：客人还在说，系统已经开始检索知识库
- 语音输入框：用户一边说，一边看到字，有错可以立刻停

**代价：** 实现更复杂（要保持一条一直开着的连接）；网络一抖，字会卡住；Chirp 3 流式官方保证的是「这一句从哪说到哪」，**不是每个字的精确时间**。

官方接口名：`StreamingRecognize`。

### 1.3 批量 BatchRecognize（本 Demo 工作台不做，见命令行 `batch_demo.py`）

**人话：** 音频已经躺在云存储里（常见是 GCS 桶里的文件），你提交一个「过夜任务」：可能几分钟到几小时后才出结果。

**业务价值：**

- 一小时研讨、培训录像、呼叫中心全天录音
- 不赶着马上看稿，但条数很多
- Chirp 3 的**说话人分离**，官方功能表把批量当作主路径

**代价：** 要先有云存储；工作台这种「本地选个文件立刻出字」的教学界面不走这条。本 Demo **不会**在你点转写时偷偷提交批量任务。

官方接口名：`BatchRecognize`（V2）。V1 里类似的叫长时间识别（long running）。接入文档：[批量识别](https://cloud.google.com/speech-to-text/docs/batch-recognize)。

工作台「本产品还有、网页 Demo 故意没接」只列 **Speech-to-Text 自己有、但网页当场做不了** 的能力：长期 Recognizer / PhraseSet 资源、Custom Speech 训练、以及上面的 BatchRecognize。分轨识别已经在工作台。Gemini 音频理解和 Live 对话是别的产品，不要写进这张表。

### 对照记一张表

| | 同步转写 | 实时听写 | 批量 |
| --- | --- | --- | --- |
| 你等多久 | 几秒到几十秒 | 边说边出字 | 任务跑完才有 |
| 音频多长 | 大约 &lt; 1 分钟 | 只要连接还在 | 分钟到小时 |
| 声音从哪来 | 请求里直接塞文件 | 麦克风分片往外送 | 云存储里的文件 |
| 本 Demo | 「转写」按钮 | 「实时听写」按钮 | 命令行，需要云存储 |
| 典型业务 | 语音消息、短留言 | 字幕、坐席辅助 | 会后出纪要、质检抽检 |

---

## 2. V1 和 V2 是什么（以及为什么要分两页）

这不是「两个完全不同的产品」，而是 **同一条 Speech-to-Text 服务的两代接口**。

可以想成：同一家速记公司，旧柜台叫 V1，新柜台叫 V2。新柜台能办的业务更多，填表方式也不一样。

### 2.1 对业务意味着什么

**新项目请走 V2 + Chirp 3。** 准确率、多语言、实时、批量，官方都把精力放在这一代。

V1 还在，是因为：

- 很多公司几年前就接好了，改接口要花钱
- **医学模型**目前在本 Demo 里按 V1 调用（口授 / 医患对话）
- 一些老字段（例如一次返回 3 个候选稿、脏话打码）在 V1 页更容易对照

所以顶栏有「V1 对照」：**用来看旧系统长什么样，不是推荐你新开一条 V1。**

### 2.2 填表有哪些肉眼可见的差别（不必背，知道「不能混」即可）

| | V2（Chirp 3 / Chirp 2 / 电话页） | V1（医学页、V1 对照页） |
| --- | --- | --- |
| 语言怎么填 | 可以填一串（字段叫 `language_codes`）：具体 locale、Chirp 3 的 `auto`，或已知语种时的清单 | 只能填一个语言码（字段叫 `language_code`） |
| 音频格式 | 常见 WAV / MP3 / WebM 可让服务自己判断 | 更稳妥的是 WAV；WebM 可能被拒 |
| 区域 | Chirp 3 用 `us` 或 `eu`；Chirp 2 用 `us-central1` | 走默认全球接口即可 |
| 「识别器」资源 | 请求路径里有 `recognizers/_`（本 Demo 用临时的 `_`，不在你项目里留下一个永久配置） | 没有这个资源 |

**区域** = 这次识别在 Google 哪一片机房跑。填错了不是「慢一点」，而是 **这个模型在那里根本不存在**（Chirp 2 发到 `us` 就会报这个错）。

**业务价值：** 选对代际和区域，等于少一次上线事故。选错模型「也能转」，但电话用会议室模型、病历用通用模型，错字会变多，质检和医生都不会买账。

---

## 3. 15 分钟上手（必须做）

打开 [本地工作台](http://127.0.0.1:8002)。每一步**只改一件事**。

### 练习 1：先看见字（3 分钟）

1. 顶栏留在 **Chirp 3**。
2. 点「第一段中文」，先点播放，确认真的在出声。
3. 点「预览请求」。**这一步不收费。** 看三件事：模型是 `chirp_3`，语言是 `cmn-Hans-CN`，区域是 `us`。
4. 点「转写」，等全文出现。

**成功：** 屏幕上的字和样例稿大体一致。  
**失败：** 看页面顶部是否还没登录云项目；模型/区域错误时，Chirp 3 应使用 `us`。

### 练习 2：只换语言（3 分钟）

导入「多语言 · English」，别的都不要动。再转写。

**成功：** 英文音频出英文。  
**记住：** 语言代码不是装饰。用普通话的代码去听英语，结果会变差。这就是为什么业务上要按产品实际用语种来配，而不是「开了识别就万能」。

### 练习 3：专有名词（4 分钟）

导入「专有名词」。先把「短语提示」清空转一次，再填上项目名、API 名转一次。

**成功：** 你能看出提示让某些词更接近清单。  
**记住：** 提示不是魔法词典，不能保证 100%。Chirp **不能**使用 V1 那种「数字类 / 人名类」的类别符号（文档里的 class token）。业务上：品牌名、工单号、人名，请显式提供清单。

### 练习 4：电话为什么要换页（3 分钟）

打开 **Telephony** 页，导入「电话 · 英语 IVR」。听：声音更窄、更像听筒。官方电话语言表没有普通话，所以这一页没有中文客服样例。8 kHz 中文对照在 Chirp 3 页「对照 · 8 kHz 中文请用 Chirp 3」。

**成功：** 你明白不是「同一个录音换个模型名字」。电话通道和会议室麦克风，训练数据不一样。  
**业务价值：** 呼叫中心质检、IVR 留言，优先电话模型；播客、会议麦，优先 Chirp 3。

### 练习 5（可选）：医学页只有英语

打开 **Medical** 页。语言下拉**只有** `en-US`。样例是英语病历口授，不是中文。

**成功：** 你不会再拿中文病历去试这一页。中文请回 Chirp 3，并用短语提示药名。

### 练习 6：分轨不是说话人分离（4 分钟）

打开 **Telephony** 页，导入「电话 · 双声道样例（一对一）」。听：左右耳不是同一个人。打开「分轨识别」再转写，结果应按 **通道 1 / 通道 2** 分开。

再导入「电话 · 四声道样例（上限 8 轨）」：API **不是只能双声道**。官方对 WAV / LINEAR16 / FLAC / OGG 是 **1–8 轨**；3 轨、4 轨同样合法。MULAW / AMR 电话编码只能 1 轨。按轨计费。

回到 **Chirp 3**，导入「对照 · 四声道样例」：同一上限，16 kHz，不是电话页专属。再对照「说话人分离」：那是**一路混音**里猜谁在说话，文件仍是单声道。

**成功：** 你能向客户讲清两件事——录音系统已经分轨，用 `channel_tag`；只有一路声音，用说话人分离。  
**记住：** 官方上限 8 轨，不是 2 轨。V1 的 `latest_short` 不能开分轨。Chirp 3 功能表没把多声道当卖点。

### 练习 7：一段录音里中日英德（4 分钟）

导入「功能 · 多语切换 · auto」。先听：四段话接在一起，不是中英术语夹杂。点「预览请求」，确认 `language_codes` 是 `["auto"]`。再转写。

再导入「功能 · 多语切换 · 列预期 locale」。预览应变成 `["cmn-Hans-CN","ja-JP"]`。Chirp 3 **最多 2 个**（官方示例 `["en-US","fr-FR"]`）；V2 多语言页写的 3 个针对 `latest_long` 等，`chirp_3` 发 3 个会 400。英、德仍在录音里。

**成功：** 你能向客户讲清——Chirp 3 **能**转多语音频；`auto` 官方转**主导语言**；已知语种时 Chirp 3 最多列 **2** 个 locale。样例稿是理想对照，不是评分器。  
**记住：** 电话页没有 `auto`。V1 的 `alternative_language_codes` 是「整段挑一种语言」，不是语码切换。

---

## 4. 它能干什么

- 把**短音频**一次性打成一篇稿（同步转写）
- 对着麦克风**边说边出字**（实时听写）
- 多种语言；Chirp 3 还可以让模型自己猜语言（`auto` 转最主要的语言）
- 已知会夹杂哪几种时，Chirp 3 可以列出预期 locale（官方示例是 `["en-US","fr-FR"]`）
- 自动加句号、问号（可关掉）
- 用短语清单提高品牌名、项目名、药名的命中率
- 给每个词标上大概几秒出现（词级时间戳；可能略降质量）
- 降噪（对有背景杂音的录音有时有用）
- 尝试标出「第 1 个人 / 第 2 个人」（说话人分离；官方对 Chirp 3 更推荐批量）
- 已经分轨的录音可以按通道分别出稿（2 轨一对一，最多 8 轨；3、4 轨同样可以）
- Chirp 2：听一种语言，直接写出另一种语言的稿（语音翻译）
- 医学口授、医患对话（**仅 en-US**，且可能要额外开通，更贵）
- 很长的录音走批量（命令行 + 云存储）

## 5. 它不能干什么

- 不能断网使用，不能把模型下载到笔记本电脑里跑
- 不能做「这是不是张三的声音」这种**声纹登录**或考勤；它只打字，不认人
- 不能保证和录音一字不差，不能当法律证据的唯一依据
- `model` 栏只能填 `chirp_3` 这种**识别模型编号**，不能填某个人的名字
- 工作台「转写」吃不下 1 小时录音
- 自定义格式提示（例如「数字一律写成阿拉伯数字」）仍是 Preview，效果不保证
- **医学模型不会识别中文病历**；未开通时，改去 Chirp 3 硬转英语药名，也不是医学模型的替代方案
- Gemini 的「听音频再聊天」是另一条产品，不是本页 Chirp
- `auto` **不能**保证一段里的每一种语言都按原文脚本出稿；Chirp 3 的 `language_codes` **不能**一次写 3 个；电话 / 医学 / V1 也没有这条 Chirp 3 多语清单路径

---

## 6. 顶栏五页分别给谁用

### Chirp 3（默认，请从这里开始）

最新、多语言、会议室/内容类音频。同步 + 实时都在这里。区域：`us` 或 `eu`。语言下拉是官方 Chirp 3 语言表全文（一百多个 locale + `auto`），不是「常用几个」。一段录音里中日英德切换，用「多语切换」样例：`auto` 或列出预期 locale。

**业务：** 会议纪要、字幕、访谈、App 里的语音转文字、多语种内容。

### Chirp 2（对照 + 翻译）

上一代多语言。要「听英语直接出中文稿」时才值得单独来。区域必须是 `us-central1`（或 europe-west4 / asia-southeast1），**不能**用 Chirp 3 的 `us`。英语→简体中文的目标码是 `cmn-Hans-CN`，不是 `zh-CN`。

**业务：** 对照旧模型、需要语音翻译而不是先转写再另调翻译接口的试点。

### Telephony（电话）

为 **8 kHz 电话**训练。客服、IVR、呼叫中心。不要拿它去听播客。

这一页**只有电话通道的模型**，不是「很多识别模型里挑一个」：

- `telephony`：常规电话录音（官方推荐名，V2 对照表只写这一个）
- `telephony_short`：很短、甚至单个词的电话语音
- `chirp_telephony`：把 Chirp 准确率用到电话；区域必须是 `us-central1`，语言只有约 10 个

短语提示（专有名词清单）在这一页**能开**，因为官方语言表给 telephony 勾了 Model adaptation。降噪**不能开**：那是 Chirp 文档里的 `denoiser_config`，不是电话模型的能力。对照表以前写「视模型」是抄错了。

语言下拉是官方 us/eu 电话语言表（约 44 个），**没有普通话**，也**没有 auto**。中文客服请用 Chirp 3（本 Demo 把 8 kHz 中文样例放在 Chirp 3 页，不放在这一页）。点「电话 · 短口令 telephony_short」可以听到短口令该用哪个模型 ID。

官方 `StreamingRecognize` **能**用 `model=telephony`。以前工作台关掉实时听写，是 Demo 自己的教学选择，不是 Google 禁止。现在这一页可以点「实时听写」测通路；浏览器麦克风是 16 kHz，**不能**代表 8 kHz 听筒音质。

坐席/客户已经分轨时，打开「分轨识别」。一对一常见 2 轨，多方会议可以 3–8 轨（样例有 4 轨）。这和说话人分离不是一回事。

**业务：** 通话质检、语音导航按键后的留言、工单自动填写。

### Medical（医学）——请看清语言

两个专用模型：

- `medical_dictation`：一位医生**独自**对着麦克风口授病历
- `medical_conversation`：医生和病人**两个人**在说话，并尝试标出谁在说

**官方只支持 `en-US`（美式英语）。** 不是「所有英语」：英式 `en-GB`、澳式 `en-AU` 也不在医学模型语言表里。普通话、粤语病历请用 Chirp 3 + 短语提示。

这是 Premium（更贵），项目里未必已经开通；失败常见 403/404，这是预期，不是 Demo 坏了。

**业务：** 英语诊所口授电子病历、英语问诊录音出稿。不要把它当成「更准的中文医疗识别」。

出处：[使用医学模型](https://docs.cloud.google.com/speech-to-text/docs/v1/medical-models)（原文：Medical models are only available for en-US）。

### V1 对照

看旧接口长什么样。新业务不要从这一页起步。短口令可以看 `latest_short`。旧电话模型叫 `phone_call`，新电话请用 Telephony 页。`latest_long` 官方语言表没有普通话；中文对照样例用的是 `default`。分轨看双声道 / 四声道样例；不要选 `latest_short`。

---

## 7. 几个开关的业务含义

入门指南页还有一张更全的表「每个开关的业务价值」。下面是口头讲解时用的短句。

- **自动标点：** 出稿能不能直接给人读。客服留言、字幕几乎都要开。
- **词级时间戳：** 字幕要对画面、质检要跳到「第 12 秒」时才需要。会略慢、略影响准确率。实时听写官方不保证每个字的时间。
- **说话人分离：** 会议纪要按人分段。结果不能当考勤机。Chirp 3 官方更强调批量。针对**单声道混音**。
- **分轨识别：** 录音文件里已经分轨。一对一通话常见 2 轨；多方会议可以 3–8 轨。结果用通道号，不是猜说话人。WAV/FLAC/OGG 上限 **8 轨**。MULAW/AMR 只能 1 轨。按轨计费。
- **短语提示：** 你们公司的产品名、工单号。没有清单，模型只能猜。
- **自动检测 / 多语切换：** `auto` 转最主要的语言。Chirp 3 列预期 locale 最多 2 个。发 3 个会 400。中英术语夹杂是另一条样例。
- **降噪：** 马路边、有音乐的录音。干净样例上几乎听不出差别。
- **语音翻译（仅 Chirp 2）：** 听 A 语言写 B 语言。不是先出英文稿再翻译；计费和错法也按「翻译」理解。

---

## 8. 样例音频从哪来

`sample_assets/audio` 里的 WAV 是电脑朗读生成的，方便没有麦克风时也能点通。**不是真人、不是诊所实录。** 转成功只说明「这条 API 通路是通的」，不能代表你们线上麦克风的效果。

---

## 9. 出问题看哪

| 你遇到的情况 | 打开 |
| --- | --- |
| Chirp 3 模型、语言、区域 | [Chirp 3 模型页](https://docs.cloud.google.com/speech-to-text/docs/models/chirp-3) |
| 医学模型是不是只有英语 | [医学模型](https://docs.cloud.google.com/speech-to-text/docs/v1/medical-models) |
| V2 还有哪些模型 | [V2 模型对照](https://cloud.google.com/speech-to-text/v2/docs/transcription-model) |
| 会花多少钱 | [定价](https://cloud.google.com/speech-to-text/pricing)（按音频时长和模型，本 Demo 不估账单） |
| 登录 / 权限 | [ADC](https://docs.cloud.google.com/docs/authentication/provide-credentials-adc) |
| 很长的录音 | [批量识别](https://cloud.google.com/speech-to-text/docs/batch-recognize) |

工作台不会悄悄提交批量任务。长音频请自己用命令行和云存储。
